#include <stdio.h>

int main(void)
{
    int numbers[] = {3, 1, 6, 8, 4, 2, 0};
    for (int z = 0; z < 7; z++)
    {
        printf("%i\n", numbers[z]);
    }
    printf("After \n");
    int i = 0;
    int j = 7;
    while (i != j)
    {
        for (int z = 0; z < j; z++)
        {
                }
    }

    for (int z = 0; z < 7; z++)
    {
        printf("%i\n", numbers[z]);
    }
}